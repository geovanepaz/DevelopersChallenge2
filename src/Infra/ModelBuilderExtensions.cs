using Microsoft.EntityFrameworkCore;

namespace Infra
{
    public static class ModelBuilderExtensions
    {
        public static void Seed(this ModelBuilder modelBuilder)
        {
            //Popular tables with initial data.
        }
    }
}
